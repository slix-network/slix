package io.natix.slix.subject.metadata;

import io.natix.slix.core.message.MessageHandler;

public enum Messages implements MessageHandler {
    PROVER_NOT_FOUND,
    PROVER_SERVICE_IS_UNAVAILABLE,
    SUBJECT_MUST_BE_REGISTER_IN_DID,
}
